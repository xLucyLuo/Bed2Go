import {  } from './../actions/modal_actions'

const modalReducer = (state = {}, action) => {
    Object.freeze(state);

    switch (action.type) {
        case "":
            ;
        case "":
        case "":
            return ;
        default:
            return state;
    }
};

export default modalReducer;